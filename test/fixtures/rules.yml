# == Schema Information
#
# Table name: rules
#
#  id               :integer          not null, primary key
#  action_operation :integer          default("assign_label"), not null
#  action_value     :string
#  filter_field     :string
#  filter_operation :integer          default("contains"), not null
#  filter_value     :string
#  created_at       :datetime
#  updated_at       :datetime
#

assign_when_ivaldi:
  filter_field: from
  filter_operation: 0 # contains
  filter_value: '@ivaldi.nl'
  action_operation: 0 # assign label
  action_value: ivaldi
