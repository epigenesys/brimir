<% Ticket.statuses.except(:merged).keys.each do |value| %>
  <%= link_to ticket_path(ticket, ticket: { status: value }),
      method: :patch, remote: true, class: 'dropdown-item' do %>
    <%= fa_icon status_icon_class(value), width: 'fw', text: t(value, scope: 'activerecord.attributes.ticket.statuses').capitalize %>
    <%=  %>
  <% end %>
<% end %>
