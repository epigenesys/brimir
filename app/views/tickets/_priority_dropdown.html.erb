<% Ticket.priorities.keys.each do |value| %>
  <%= link_to ticket_path(ticket, ticket: { priority: value }, short: short),
      method: :patch, remote: true, class: 'dropdown-item' do %>
    <i class="cc-priority-<%= value %> far fa-fw fa-circle"></i>
    <%= t(value) %>
  <% end %>
<% end %>
