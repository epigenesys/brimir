<div class="container">
  <div class="card">
    <div class="card-header">
      <%= link_to t(:delete_rule), rule_path(@rule), 
            method: :delete, 
            data: { confirm: t(:are_you_sure) },
            class: 'btn btn-danger float-right' %>
      <h2 class="mb-0"><%= t(:new_rule) %></h2>
    </div>
    <div class="card-body">
      <%= render 'form' %>
    </div>
  </div>
</div>